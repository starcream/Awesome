extern -- 只初始化一次，不能在函数内部声明，用来声明对外部变量的权限

static -- 初始化一次，默认0；和普通变量作用域一样，但是函数执行结束后，不会销毁；存储在静态存储区


普通全局变量可以跨文件所见，如果是静态的全局变量，不能被extern从外部访问。原因很简单，static只在本文件中可见
同样 ，静态函数也只在本文件中可见。
所以，使用静态关键字的好处之一，就是可以在不同文件中定义相同名字的函数，不会冲突

类中如果有静态成员，可以为类的所有对象共享，但必须类外初始化 --- 个人理解：否则每创建一个对象都会初始化一次
