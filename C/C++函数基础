参数传递 -- 传值，传引用
Java 默认传引用


传值- 复制实参的值而已
传引用 - 可以改变参数值

函数的默认参数，有默认值的参数放后面

数组默认是传引用的，如果不想数组参数被改变， 可以加const关键字  void helper(const float A[]);

传递多维数组时，第一维的大小可省略，后面不行

C++ 库中都有对应头文件  -- 函数原型，数据结构定义，常量定义


#include<cmath>

sqrt()

注意，如果要包含自己写的头文件，用""，#include<hello.h> 
如果是系统的，用<>
主要差异是编译器中预处理器的搜索方式不一样，如果用双引号，会优先从当前目录开始搜索
头文件中不放全局变量和函数的实现
